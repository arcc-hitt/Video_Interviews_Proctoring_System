# Example env for local/dev. Copy to .env.development or .env as needed.
# Note: Vite exposes VITE_* variables to the client; do not put secrets here.

VITE_API_BASE_URL=http://localhost:5000
VITE_WS_URL=ws://localhost:5000
VITE_HISTORY_REFRESH_ATTEMPTS=3
VITE_HISTORY_REFRESH_INTERVAL_MS=4000
